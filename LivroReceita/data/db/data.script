CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE INGREDIENTES(CODINGREDIENTE INTEGER NOT NULL PRIMARY KEY,DESCRICAO VARCHAR(255),RECEITA INTEGER NOT NULL)
CREATE MEMORY TABLE RECEITAS(COD_RECEITA INTEGER NOT NULL PRIMARY KEY,NOME VARCHAR(255),MODOPREPARO VARCHAR(255),OBSERVACOES VARCHAR(255),TIPO VARCHAR(2))
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 10
